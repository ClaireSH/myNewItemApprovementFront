import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#292961",
      light: "#57518f",
      dark: "#000037",
    },
    secondary: {
      main: "#00DFB0",
      light: "#65ffe2",
      dark: "#00ac81",
    },
  },
});

export default theme;
